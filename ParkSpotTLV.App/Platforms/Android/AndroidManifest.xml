<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.parkspot.tlv"> 
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

	<application
		android:usesCleartextTraffic="true"
		android:allowBackup="true"
		android:icon="@mipmap/appicon"
		android:roundIcon="@mipmap/appicon_round"
		android:supportsRtl="true">


		<!-- BELONGS TO TOMMER ! -->
		<meta-data
			android:name="com.google.android.geo.API_KEY"
			android:value="${mapsApiKey}"/>

		<!-- meta-data
			android:name="com.google.android.geo.API_KEY"
			android:value="" />
			מיכל אל תשימי פה את המפתח, זה מופיע לכולם באינטנרט ובסוף מישהו יגנוב את זה ויהיה לך חיוב של אלפי דולרים בגוגל 
			תבדקי עם הצ'אט איך אפשר להסתיר את זה, בווינדוז אני שמתי את זה כמשתנה סביבה, אבל אני לא מכיר את הפעולות במאק
			פשוט צריך לוודא שהמפתח לא מופיע באף קובץ
			-->
	</application>
</manifest>
